import { Router } from "express"; import { requireAuth } from "../middleware/auth"; import { addTier, createEvent, dashboard, deleteEvent, getPublicEvent, listTiers, updateEvent } from "../controllers/eventController"; const router = Router(); router.post("/", requireAuth, createEvent); router.patch("/:id", requireAuth, updateEvent); router.delete("/:id", requireAuth, deleteEvent); router.post("/:id/tiers", requireAuth, addTier); router.get("/:id/tiers", listTiers); router.get("/:id/dashboard", requireAuth, dashboard); router.get("/:slug", getPublicEvent); export default router;
