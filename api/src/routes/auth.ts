import { Router } from "express"; import { login, signup } from "../controllers/authController"; import { connectGoogle, disconnectGoogle, googleCallback } from "../controllers/googleAuthController"; import { requireAuth } from "../middleware/auth"; const router = Router(); router.post("/signup", signup); router.post("/login", login); router.get("/google/connect", requireAuth, connectGoogle); router.get("/google/callback", googleCallback); router.post("/google/disconnect", requireAuth, disconnectGoogle); export default router;
