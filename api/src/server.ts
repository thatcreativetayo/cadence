import express from "express"; import cors from "cors"; import mongoose from "mongoose"; import { config } from "./config"; import authRoutes from "./routes/auth"; import eventRoutes from "./routes/events"; import ticketRoutes from "./routes/tickets"; import paymentRoutes from "./routes/payments"; import { errorHandler } from "./middleware/errors";
const app = express(); app.use(cors()); app.use(express.json({ verify: (req, _res, buffer) => { (req as express.Request).rawBody = Buffer.from(buffer); } })); app.get("/health", (_req, res) => res.json({ status: "ok" })); app.use("/auth", authRoutes); app.use("/events", eventRoutes); app.use("/tickets", ticketRoutes); app.use("/payments", paymentRoutes); app.use(errorHandler);
mongoose.connect(config.mongoUri).then(() => app.listen(config.port, () => console.log(`Cadence API listening on http://localhost:${config.port}`))).catch(err => { console.error("MongoDB connection failed", err); process.exit(1); });
export default app;
